// Global test setup
beforeAll(async () => {
  // Setup test database if needed
  console.log('🧪 Setting up tests...');
});

afterAll(async () => {
  // Cleanup
  console.log('✅ Tests completed');
});
